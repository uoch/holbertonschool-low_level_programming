#include "main.h"
/**
 * times_table - prints an n times table.
 *
 */
void times_table(void)
{
	int i, j
	for (i = 0; j <= 9; a++)
	{
		for (j = 0; j <= 9; b++)
		{
			_putchar( i * j)
		}
	}
	_putchar(10);
}
