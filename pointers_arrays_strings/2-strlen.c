#include "main.h"

/**
 * swap_int - swaps the values of int a and int b
 * @a: first int to swap
 * @b: second int to swap
 *
 * Return: void
 */
int _strlen(char *s)
{
    int i = 0;
    
    while ( s[i] != '\0')
        i++;
    return(i);

}